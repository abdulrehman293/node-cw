# Copy to .env.local for local dev. Never commit the real .env file.
# On Cloudways Velocity, set this in the Environment Variables tab instead.

# Point this straight at your Postgres database, not a serverless pooler.
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME
